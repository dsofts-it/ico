# Flutter App - Complete User Flow Diagram

## 🎯 App Flow Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        SPLASH SCREEN                             │
│                    Check Authentication                          │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ├─── Token Exists? ───┐
                 │                     │
                 NO                   YES
                 │                     │
                 ▼                     ▼
        ┌────────────────┐    ┌────────────────┐
        │  LOGIN SCREEN  │    │  HOME SCREEN   │
        └────────────────┘    └────────────────┘
```

---

## 🔐 Authentication Flow

### **New User Journey**

```
┌──────────────────────┐
│  MOBILE SIGNUP       │
│  - Enter Name        │
│  - Enter Mobile      │
│  - Click "Send OTP"  │
└──────────┬───────────┘
           │
           │ API: POST /auth/signup/mobile-init
           │ Body: { name, mobile }
           │ Response: { userId }
           │
           ▼
┌──────────────────────┐
│  OTP VERIFICATION    │
│  - Enter 6-digit OTP │
│  - Resend option     │
│  - Click "Verify"    │
└──────────┬───────────┘
           │
           │ API: POST /auth/signup/verify
           │ Body: { userId, otp, type: "mobile" }
           │ Response: { token, user }
           │ Store: JWT Token + User Data
           │
           ▼
┌──────────────────────┐
│  PIN SETUP           │
│  - Enter 4-digit PIN │
│  - Confirm PIN       │
│  - Click "Setup"     │
└──────────┬───────────┘
           │
           │ API: POST /auth/pin/setup
           │ Headers: Authorization: Bearer {token}
           │ Body: { pin }
           │
           ▼
┌──────────────────────┐
│  HOME SCREEN         │
│  (Authenticated)     │
└──────────────────────┘
```

---

### **Returning User Journey**

```
┌──────────────────────┐
│  LOGIN SCREEN        │
│  - Enter Mobile      │
│  - Choose Method     │
└──────────┬───────────┘
           │
           ├─────────────────┬─────────────────┐
           │                 │                 │
      PIN LOGIN         OTP LOGIN        EMAIL LOGIN
           │                 │                 │
           ▼                 ▼                 ▼
┌──────────────────┐  ┌──────────────┐  ┌──────────────┐
│ PIN LOGIN SCREEN │  │ OTP SENT     │  │ EMAIL LOGIN  │
│ - Enter PIN      │  │ - Enter OTP  │  │ (Future)     │
│ - Click "Login"  │  │ - Verify     │  └──────────────┘
└────────┬─────────┘  └──────┬───────┘
         │                   │
         │ POST /auth/       │ POST /auth/
         │ login/pin         │ login/mobile-verify
         │                   │
         └────────┬──────────┘
                  │
                  ▼
         ┌────────────────┐
         │  HOME SCREEN   │
         └────────────────┘
```

---

## 🏠 Home Screen Navigation

```
┌─────────────────────────────────────────────────────────────┐
│                      HOME SCREEN                            │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │   WALLET    │  │  ICO TOKENS │  │   PROFILE   │       │
│  │   BALANCE   │  │   TRADING   │  │   SECTION   │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                             │
│  Bottom Navigation:                                         │
│  [Home] [Wallet] [ICO] [Profile]                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 💰 Wallet Section Flow

### **Wallet Dashboard**

```
┌──────────────────────────────────────┐
│       WALLET DASHBOARD               │
│                                      │
│  Current Balance: ₹1,000             │
│  Total Tokens: 100 ICOX              │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  [+ Add Money]  [Transaction] │ │
│  └────────────────────────────────┘ │
│                                      │
│  Recent Transactions:                │
│  ┌────────────────────────────────┐ │
│  │ Buy  | 50 ICOX | ₹500 | ✓     │ │
│  │ Sell | 10 ICOX | ₹100 | ⏳    │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
         │
         ├─── Click "Add Money" ───┐
         │                         │
         ▼                         │
┌──────────────────────┐          │
│  ADD MONEY SCREEN    │          │
│  - Enter Amount      │          │
│  - Click "Proceed"   │          │
└──────────┬───────────┘          │
           │                       │
           │ Create Order          │
           │ POST /orders          │
           │ Response: {           │
           │   paymentSession      │
           │ }                     │
           ▼                       │
┌──────────────────────┐          │
│  PHONEPE PAYMENT     │          │
│  (WebView)           │          │
│  - User pays         │          │
│  - Redirect back     │          │
└──────────┬───────────┘          │
           │                       │
           │ Payment Success       │
           ▼                       │
┌──────────────────────┐          │
│  SUCCESS SCREEN      │          │
│  - Show confirmation │          │
│  - Update balance    │          │
└──────────┬───────────┘          │
           │                       │
           └───────────────────────┘
                     │
                     ▼
           Back to Wallet Dashboard
```

---

## 🪙 ICO Trading Flow

### **ICO Dashboard**

```
┌──────────────────────────────────────────┐
│         ICO DASHBOARD                    │
│                                          │
│  Token: ICOX                             │
│  Current Price: ₹10 per token            │
│                                          │
│  Your Holdings:                          │
│  ┌────────────────────────────────────┐ │
│  │  Total Tokens: 100 ICOX            │ │
│  │  Invested: ₹1,000                  │ │
│  │  Current Value: ₹1,000             │ │
│  │  Profit/Loss: ₹0 (0%)              │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌──────────────┐  ┌──────────────┐    │
│  │  [BUY TOKENS]│  │ [SELL TOKENS]│    │
│  └──────────────┘  └──────────────┘    │
└──────────────────────────────────────────┘
         │                    │
         │                    │
    Click Buy            Click Sell
         │                    │
         ▼                    ▼
┌──────────────────┐  ┌──────────────────┐
│ BUY TOKENS       │  │ SELL TOKENS      │
│ - Enter Tokens   │  │ - Enter Tokens   │
│ - Show Amount    │  │ - Show Amount    │
│ - Click "Buy"    │  │ - Click "Sell"   │
└────────┬─────────┘  └────────┬─────────┘
         │                      │
         │ POST /ico/buy        │ POST /ico/sell
         │ Response: {          │ Response: {
         │   paymentSession     │   transaction
         │ }                    │ }
         ▼                      │
┌──────────────────┐           │
│ PHONEPE PAYMENT  │           │
│ (Same as wallet) │           │
└────────┬─────────┘           │
         │                      │
         │ Success              │
         ▼                      ▼
┌──────────────────────────────────┐
│  TRANSACTION CONFIRMED           │
│  - Update holdings               │
│  - Show new balance              │
└──────────────────────────────────┘
```

---

## 👤 Profile Section Flow

```
┌──────────────────────────────────────┐
│        PROFILE SCREEN                │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  Name: John Doe                │ │
│  │  Mobile: +91 9876543210        │ │
│  │  Verified: ✓                   │ │
│  └────────────────────────────────┘ │
│                                      │
│  Options:                            │
│  ┌────────────────────────────────┐ │
│  │  📍 Manage Addresses           │ │
│  │  🔒 Change PIN                 │ │
│  │  📊 Transaction History        │ │
│  │  ℹ️  About                     │ │
│  │  🚪 Logout                     │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
         │
         │ Click "Manage Addresses"
         ▼
┌──────────────────────────────────────┐
│     ADDRESS LIST SCREEN              │
│                                      │
│  ┌────────────────────────────────┐ │
│  │ 🏠 Home (Default)              │ │
│  │ 123 Main St, Mumbai            │ │
│  │ [Edit] [Delete]                │ │
│  └────────────────────────────────┘ │
│                                      │
│  ┌────────────────────────────────┐ │
│  │ 🏢 Office                      │ │
│  │ 456 Business Park, Mumbai      │ │
│  │ [Edit] [Delete] [Set Default]  │ │
│  └────────────────────────────────┘ │
│                                      │
│  [+ Add New Address]                 │
└──────────────────────────────────────┘
         │
         │ Click "Add New Address"
         ▼
┌──────────────────────────────────────┐
│   ADD/EDIT ADDRESS SCREEN            │
│                                      │
│  Label: [Home/Office/Other]          │
│  Full Name: [____________]           │
│  Phone: [____________]               │
│  Address Line 1: [____________]      │
│  Address Line 2: [____________]      │
│  City: [____________]                │
│  State: [____________]               │
│  Postal Code: [____________]         │
│  Landmark: [____________]            │
│                                      │
│  [ ] Set as default address          │
│                                      │
│  [Save Address]                      │
└──────────────────────────────────────┘
         │
         │ POST /user/addresses
         │ Body: { address data }
         ▼
    Address Saved
    Back to Address List
```

---

## 📊 Complete API Flow Map

### **Authentication APIs**

```
┌─────────────────────────────────────────────────────────┐
│                  AUTHENTICATION FLOW                    │
└─────────────────────────────────────────────────────────┘

1. SIGNUP
   POST /auth/signup/mobile-init
   ↓
   POST /auth/signup/verify
   ↓
   POST /auth/pin/setup

2. LOGIN
   POST /auth/login/pin
   OR
   POST /auth/login/mobile-init → POST /auth/login/mobile-verify
```

### **Wallet & ICO APIs**

```
┌─────────────────────────────────────────────────────────┐
│                    WALLET FLOW                          │
└─────────────────────────────────────────────────────────┘

1. VIEW BALANCE
   GET /ico/summary (requires auth)

2. VIEW TRANSACTIONS
   GET /ico/transactions (requires auth)

3. ADD MONEY
   POST /orders (requires auth)
   ↓
   PhonePe Payment
   ↓
   Webhook: POST /payments/phonepe/callback

4. BUY TOKENS
   GET /ico/price (public)
   ↓
   POST /ico/buy (requires auth)
   ↓
   PhonePe Payment

5. SELL TOKENS
   POST /ico/sell (requires auth)
```

### **Profile APIs**

```
┌─────────────────────────────────────────────────────────┐
│                   PROFILE FLOW                          │
└─────────────────────────────────────────────────────────┘

1. GET ADDRESSES
   GET /user/addresses (requires auth)

2. ADD ADDRESS
   POST /user/addresses (requires auth)

3. UPDATE ADDRESS
   PUT /user/addresses/:id (requires auth)

4. DELETE ADDRESS
   DELETE /user/addresses/:id (requires auth)

5. SET DEFAULT
   PATCH /user/addresses/:id/default (requires auth)
```

---

## 🔄 State Management Flow

```
┌─────────────────────────────────────────────────────────┐
│                  PROVIDER PATTERN                       │
└─────────────────────────────────────────────────────────┘

AuthProvider
├── User State
├── Authentication Status
├── Loading State
└── Error State

WalletProvider
├── Balance
├── Transactions
├── Loading State
└── Error State

UserProvider
├── Addresses
├── Profile Data
├── Loading State
└── Error State

UI Screens
    ↓
Consumer<Provider>
    ↓
Display Data / Handle Actions
    ↓
Update UI on State Change
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────┐
│                  SECURITY MEASURES                      │
└─────────────────────────────────────────────────────────┘

1. TOKEN STORAGE
   JWT Token → flutter_secure_storage

2. API REQUESTS
   All authenticated requests:
   Headers: { Authorization: "Bearer {token}" }

3. TOKEN EXPIRY
   401 Response → Clear Token → Redirect to Login

4. LOGOUT
   Clear all stored data → Navigate to Login

5. PIN SECURITY
   PIN hashed on backend
   Never stored in plain text
```

---

## 📱 Screen Navigation Map

```
┌─────────────────────────────────────────────────────────┐
│                  NAVIGATION STRUCTURE                   │
└─────────────────────────────────────────────────────────┘

SplashScreen
    ↓
    ├─── Not Authenticated ───→ LoginScreen
    │                              ├─→ MobileSignupScreen
    │                              │      ↓
    │                              │   OTPVerificationScreen
    │                              │      ↓
    │                              │   PINSetupScreen
    │                              │      ↓
    │                              └─→ PINLoginScreen
    │
    └─── Authenticated ───→ HomeScreen (Bottom Nav)
                               ├─→ WalletScreen
                               │      ├─→ AddMoneyScreen
                               │      │      ↓
                               │      │   PaymentWebViewScreen
                               │      └─→ TransactionHistoryScreen
                               │
                               ├─→ ICODashboardScreen
                               │      ├─→ BuyTokensScreen
                               │      │      ↓
                               │      │   PaymentWebViewScreen
                               │      └─→ SellTokensScreen
                               │
                               └─→ ProfileScreen
                                      ├─→ EditProfileScreen
                                      └─→ AddressListScreen
                                             ├─→ AddAddressScreen
                                             └─→ EditAddressScreen
```

---

## 🎨 UI Component Hierarchy

```
┌─────────────────────────────────────────────────────────┐
│                  WIDGET STRUCTURE                       │
└─────────────────────────────────────────────────────────┘

MaterialApp
└── MultiProvider
    ├── AuthProvider
    ├── WalletProvider
    └── UserProvider
        └── Router
            ├── SplashScreen
            ├── Auth Screens
            │   ├── CustomTextField
            │   ├── CustomButton
            │   └── OTPInputField
            ├── Home Screens
            │   ├── BottomNavigationBar
            │   ├── WalletCard
            │   ├── TransactionCard
            │   └── TokenCard
            └── Profile Screens
                ├── AddressCard
                └── ProfileListTile
```

---

## 🚀 App Lifecycle

```
┌─────────────────────────────────────────────────────────┐
│                   APP LIFECYCLE                         │
└─────────────────────────────────────────────────────────┘

App Start
    ↓
Initialize Providers
    ↓
Check Stored Token
    ↓
    ├─── Token Valid ───→ Load User Data → Home Screen
    │
    └─── No Token ───→ Login Screen

On Login Success
    ↓
Store Token & User Data
    ↓
Navigate to Home
    ↓
Load Wallet Data
    ↓
Load Transactions

On Logout
    ↓
Clear All Stored Data
    ↓
Reset Providers
    ↓
Navigate to Login Screen
```

---

## ⚡ Performance Optimization

```
┌─────────────────────────────────────────────────────────┐
│              OPTIMIZATION STRATEGIES                    │
└─────────────────────────────────────────────────────────┘

1. CACHING
   - Cache user data locally
   - Cache token price
   - Use shared_preferences for non-sensitive data

2. LAZY LOADING
   - Load transactions on demand
   - Paginate transaction history
   - Load addresses when needed

3. STATE MANAGEMENT
   - Use Provider for reactive updates
   - Avoid unnecessary rebuilds
   - Use const constructors

4. NETWORK
   - Implement retry logic
   - Handle timeouts
   - Show cached data while loading

5. UI
   - Use ListView.builder for lists
   - Implement pull-to-refresh
   - Show shimmer loading effects
```

---

## 📋 Testing Checklist

```
✅ Authentication
   ├─ Mobile signup with OTP
   ├─ OTP verification
   ├─ PIN setup
   ├─ PIN login
   └─ OTP login

✅ Wallet
   ├─ View balance
   ├─ Add money
   ├─ PhonePe payment
   └─ Transaction history

✅ ICO Trading
   ├─ View token price
   ├─ Buy tokens
   ├─ Sell tokens
   └─ View holdings

✅ Profile
   ├─ View profile
   ├─ Add address
   ├─ Edit address
   ├─ Delete address
   └─ Set default address

✅ Error Handling
   ├─ Network errors
   ├─ Invalid credentials
   ├─ Expired OTP
   ├─ Payment failures
   └─ Token expiry

✅ UI/UX
   ├─ Loading states
   ├─ Error messages
   ├─ Success confirmations
   └─ Smooth animations
```

---

**This flow diagram provides a complete visual guide for implementing the Flutter app! 🎯**
